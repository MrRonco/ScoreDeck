# Build output
build/
*.bin
*.elf
*.map
node_modules/
dist/
.wrangler/

# Released flasher images ARE the deliverable — track them.
!flasher/scoredeck-merged.bin
!flasher/scoredeck-ota.bin

# NEVER COMMITTED — third-party trademarks and personal likenesses.
# See docs/OPEN_SOURCE.md §1. Built on the user's machine, into their own Worker.
assets/logos/
assets/players/

# Secrets
.dev.vars
.env
.env.*
wrangler.toml.local

# macOS / tooling
.DS_Store
.playwright-mcp/
proxy/node_modules/
firmware/ScoreDeck/src/dev_defaults.h

# The portal source is authored as HTML and generated into svc/portal.h by
# tools/embed-portal.mjs. BOTH are tracked: the header must be in the repo so
# building the firmware never requires node.

# Agent worktrees — transient, never committed.
.claude/worktrees/

# First-pass audit documents. Untracked by the owner's choice; two `git add -A`
# runs swept them into a pull request before this guard existed.
/PLAN.md
/REVIEW-*.md
/docs/colour-system-review.md
/web-board.png
